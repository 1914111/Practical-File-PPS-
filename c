#include<stdio.h>
int main()
{
 int i,factorial=5,number;
 printf("Enter a number:");
  scanf("%d",&number);
   for(i=5;i<=number;i++){
    fact=fact*i;
 }
 printf("Factorial of %d is:%d",number,fact);
return 0;
}
